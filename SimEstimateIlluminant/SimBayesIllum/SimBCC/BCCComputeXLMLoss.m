function [xLMLoss] = BCCComputeXLMLoss(xHat,xTrue,Klx)%	[xLMLoss] = BCCComputeXLMLoss(xHat,xTrue,Klx)%% Compute the local mass loss.%% 10/30/94	dhb		Wrote it.% 11/21/94	dhb		Changed argument order.%		dhb		Compute result on an input matrix.[nil,n] = size(xHat);xLMLoss = zeros(1,n);for i = 1:n	delta = xHat(:,i)-xTrue(:,i);	xLMLoss(i) = 1 - exp(-0.5*(delta'*inv(Klx)*delta));end